eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/

---
spring:
  config:
    activate:
      on-profile: article
  application:
    name: lab-4-article
words: a,the

---
spring:
  config:
    activate:
      on-profile: sentence
  application:
    name: lab-4-sentence
server:
  port: 8020
words: notApplicable
