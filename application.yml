eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/

---
spring:
  config:
    activate:
      on-profile: article
words: a,the

---
spring:
  config:
    activate:
      on-profile: sentence
server:
  port: 8020
words: notApplicable
