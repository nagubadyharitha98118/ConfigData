eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/

---
spring:
  profiles: article
words: a,the

---
spring:
  config:
    activate:
      on-profile: sentence
server:
  port: 8020
words: notApplicable
